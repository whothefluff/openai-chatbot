package com.openai.chatbot.application.adapter.rest.domainintegration;

/**
 * DTO for a previous message
 */
@SuppressWarnings( "ClassWithoutLogger" )
public class HistoryMessage{
  //type (assistant, system, user)
  //message
  //timestamp
  //total tokens (only for assistant)
  //model and temperature (only for user)
}
